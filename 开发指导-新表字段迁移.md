# 周报抽取结果迁移至新表 — 分阶段开发指导

> 本文档用于指导 Claude Code / Codex 按阶段完成新表字段迁移开发。
> 每个阶段完成后**必须停止**，等待人工验收通过后再继续下一阶段。
>
> 生成日期：2026-05-14
> 执行前必须已阅读：`docs/ai/project/context.md`、`docs/ai/project/rules.md`

---

## 变更概览

**目标**：将周报抽取结果从旧表（`hERWDMS`）迁移至新表（`XD9FUh1`，baseId: `ZX6GRezwJl7DbxOxirwj3wGzVdqbropQ`）。

**核心变化**：
- LLM 提示词：进度抽取增加 6 个新字段（项目名称、项目编号、周报周期、进度值、里程碑、本周小结、下周计划）；风险抽取改为 6 列结构（风险类型、风险等级、风险描述、应对措施、责任人、计划解决日期）
- 删除隐藏风险抽取环节
- 钉钉同步：新增 `progress` 字段类型处理；更新风险文本格式化顺序；date 字段增加截取日期逻辑
- 配置文件：整份重写为 11 个字段

**不修改的模块**：`service.py`（payload 结构兼容）、`email_parser.py`、`llm_client.py`、`runtime.py`、前端静态资源

---

## 阶段 1：配置文件 + 默认字段

### 修改文件

1. `weekly_report_platform/resources/config.local.json`
2. `weekly_report_platform/infrastructure/config.py`（`build_default_fields()`）

### config.local.json 内容

将现有内容替换为：

```json
{
  "fields": {
    "message_id": {
      "fieldName": "邮件标识",
      "type": "text",
      "required": false,
      "sourcePath": "message_id"
    },
    "project_name": {
      "fieldName": "项目名称",
      "type": "text",
      "required": true,
      "sourcePath": "analysis.进度抽取结果.项目名称"
    },
    "project_code": {
      "fieldName": "项目编号",
      "type": "text",
      "required": true,
      "sourcePath": "analysis.进度抽取结果.项目编号"
    },
    "project_cycle": {
      "fieldName": "项目周报周期",
      "type": "text",
      "required": true,
      "sourcePath": "analysis.进度抽取结果.项目周报周期"
    },
    "project_progress": {
      "fieldName": "项目进度",
      "type": "progress",
      "required": true,
      "sourcePath": "analysis.进度抽取结果.项目进度"
    },
    "milestone": {
      "fieldName": "里程碑",
      "type": "text",
      "required": true,
      "sourcePath": "analysis.进度抽取结果.里程碑"
    },
    "risk_details": {
      "fieldName": "风险详情",
      "type": "text",
      "required": false,
      "sourcePath": "analysis.风险抽取结果.风险详情"
    },
    "overall_progress": {
      "fieldName": "项目整体进展",
      "type": "text",
      "required": false,
      "sourcePath": "analysis.进度抽取结果.综合总结"
    },
    "weekly_summary": {
      "fieldName": "本周小结",
      "type": "text",
      "required": false,
      "sourcePath": "analysis.进度抽取结果.本周小结"
    },
    "next_week_plan": {
      "fieldName": "下周计划",
      "type": "text",
      "required": false,
      "sourcePath": "analysis.进度抽取结果.下周计划"
    },
    "extract_date": {
      "fieldName": "邮箱抽取日期",
      "type": "date",
      "required": false,
      "sourcePath": "processed_at"
    }
  },
  "targets": [
    {
      "baseId": "ZX6GRezwJl7DbxOxirwj3wGzVdqbropQ",
      "tableId": "XD9FUh1"
    }
  ]
}
```

### config.py 修改

`build_default_fields()` 返回值替换为与上述 `config.local.json` 中 `"fields"` 相同的内容（不含 `"targets"`）。

### 测试修改

**`tests/test_wrp_config.py`**

- `test_wrp_load_dingtalk_targets_reads_project_env` 第 31 行：将 `progress_analysis` 的断言改为 `project_progress` 或 `project_name` 的 `sourcePath`。具体检查这行：
  ```python
  assert targets[0]["fields"]["progress_analysis"]["sourcePath"] == "analysis.进度抽取结果.分析结果"
  ```
  改为：
  ```python
  assert targets[0]["fields"]["project_progress"]["sourcePath"] == "analysis.进度抽取结果.项目进度"
  ```

### 验收标准

- `config.local.json` 是合法 JSON，包含 11 个字段定义，`targets` 指向新表。
- `config.py` 的 `build_default_fields()` 返回 11 个字段，与 `config.local.json` 的 `fields` 一致。
- `test_wrp_config.py` 中 6 个测试全部通过。
- `test_wrp_default_config_file_contains_targets` 仍然通过（新配置仍然包含 `targets` 数组）。

---

## 阶段 2：LLM 提示词 — 进度抽取 + 删除隐藏风险

### 修改文件

- `weekly_report_platform/domain/mail_analysis/prompts.py`

### 2.1 新增常量

在现有常量块后追加：

```python
PROJECT_NAME_KEY = "项目名称"
PROJECT_CODE_KEY = "项目编号"
PROJECT_CYCLE_KEY = "项目周报周期"
PROJECT_PROGRESS_KEY = "项目进度"
MILESTONE_KEY = "里程碑"
WEEKLY_SUMMARY_KEY = "本周小结"
NEXT_WEEK_PLAN_KEY = "下周计划"
```

### 2.2 修改进度抽取提示词

将 `get_progress_extract_prompt()` 替换为以下内容：

```python
def get_progress_extract_prompt(formatted_full_text: str) -> str:
    """生成进度抽取提示词。"""
    return f"""
You are extracting weekly report progress information from email content and attachments.
Return strict JSON with the exact keys below.

Rules:
- Preserve important source text excerpts when possible.
- If only one source exists, set consistency analysis to "Not applicable".
- Focus on summary/progress sections only.
- For "项目进度", extract a number between 0 and 1 representing the project completion percentage.
  Look for progress indicators like "项目PC", "项目进度" in the email body or attachments.
  Convert percentage values (e.g., "75%" -> 0.75) to decimal form.
- For other fields, extract from the email content and attachments as appropriate.

Input:
{formatted_full_text}

Output JSON schema:
{{
  "{RAW_EXTRACT_KEY}": [
    {{"{SOURCE_KEY}": "body-or-attachment", "{CONTENT_KEY}": "excerpt"}}
  ],
  "{ANALYSIS_RESULT_KEY}": {{
    "{SUMMARY_KEY}": "concise summary",
    "{CONSISTENCY_KEY}": "consistency result"
  }},
  "{PROJECT_NAME_KEY}": "project name",
  "{PROJECT_CODE_KEY}": "project code",
  "{PROJECT_CYCLE_KEY}": "project weekly report cycle (e.g. 2026-05-12 ~ 2026-05-18)",
  "{PROJECT_PROGRESS_KEY}": 0.75,
  "{MILESTONE_KEY}": "key milestone information",
  "{WEEKLY_SUMMARY_KEY}": "this week summary",
  "{NEXT_WEEK_PLAN_KEY}": "next week plan"
}}
""".strip()
```

### 2.3 删除隐藏风险

- 删除 `HIDDEN_RISK_KEY` 常量。
- 删除 `get_hidden_risk_extract_prompt()` 函数。
- 删除 `extract_hidden_risk_from_report()` 函数。
- 修改 `process_email_analysis_with_errors()`：
  - 删除 `hidden_risk_result, hidden_risk_error = await extract_hidden_risk_from_report(...)` 这一行。
  - 删除 `if hidden_risk_result is not None: result[HIDDEN_RISK_KEY] = hidden_risk_result` 块。
  - 删除 `if hidden_risk_error is not None: errors.append(hidden_risk_error)` 块。

### 测试修改

**`tests/test_wrp_prompts.py`**

- 删除 `test_wrp_extract_hidden_risk_from_report_ignores_invalid_raw_extract_shape` 整个测试函数。
- 保留 `test_wrp_extract_progress_from_report_rejects_non_object_response`，确保它仍通过。

### 验收标准

- `prompts.py` 中不再有 `HIDDEN_RISK_KEY`、`get_hidden_risk_extract_prompt`、`extract_hidden_risk_from_report`。
- `process_email_analysis_with_errors()` 只剩进度 + 风险两段调用（`asyncio.gather`）。
- `test_wrp_prompts.py` 中剩余测试通过。
- 生成的进度提示词中包含 `项目名称`、`项目编号`、`项目周报周期`、`项目进度`、`里程碑`、`本周小结`、`下周计划` 的 JSON key 定义。

---

## 阶段 3：LLM 提示词 — 风险抽取

### 修改文件

- `weekly_report_platform/domain/mail_analysis/prompts.py`（继续修改）

### 3.1 新增风险常量

在阶段 2 的常量后追加：

```python
RISK_LEVEL_KEY = "风险等级"
RISK_DUE_DATE_KEY = "计划解决日期"
```

### 3.2 修改风险抽取提示词

将 `get_risk_extract_prompt()` 替换为：

```python
def get_risk_extract_prompt(formatted_full_text: str) -> str:
    """生成显式风险抽取提示词。"""
    return f"""
You are extracting explicit weekly report risks from email content and attachments.
Return strict JSON with the exact keys below.

Input:
{formatted_full_text}

Output JSON schema:
{{
  "{RAW_EXTRACT_KEY}": [
    {{"{SOURCE_KEY}": "body-or-attachment", "{CONTENT_KEY}": "risk excerpt"}}
  ],
  "{CONSISTENCY_KEY}": "consistency result",
  "{RISK_DETAIL_KEY}": [
    {{
      "{SEQ_KEY}": 1,
      "{RISK_TYPE_KEY}": "risk type",
      "{RISK_LEVEL_KEY}": "high/medium/low",
      "{RISK_DESC_KEY}": "risk description",
      "{RISK_ACTION_KEY}": "mitigation measures",
      "{RISK_OWNER_KEY}": "owner",
      "{RISK_DUE_DATE_KEY}": "planned resolution date"
    }}
  ]
}}
""".strip()
```

**关键变化**：风险详情数组中每个对象的结构从原来的 5 个字段变为 7 个字段：
- 保留：序号、风险描述、风险类型、风险措施和最新进展、风险责任人
- 新增：风险等级、计划解决日期
- 排列顺序：序号 → 风险类型 → 风险等级 → 风险描述 → 应对措施 → 责任人 → 计划解决日期

### 验收标准

- `get_risk_extract_prompt()` 输出的 JSON schema 包含 7 个风险详情字段。
- 风险详情中不再包含旧字段"风险归纳"（`RISK_SUMMARY_KEY`，该常量属于隐藏风险，已在阶段 2 删除）。
- 运行 `python -m pytest -q --import-mode=importlib tests/test_wrp_prompts.py` 通过。

---

## 阶段 4：钉钉同步层 — progress 类型 + 风险格式化 + date 处理

### 修改文件

- `weekly_report_platform/dingtalk.py`

### 4.1 删除隐藏风险相关常量

- 删除 `HIDDEN_RISK_PATH` 常量。
- 删除 `RISK_SUMMARY_KEY` 常量。

### 4.2 修改 `PROGRESS_PATH`

将 `PROGRESS_PATH` 重命名为 `PROGRESS_VALUE_PATH`，并更新值：

```python
PROGRESS_VALUE_PATH = "analysis.进度抽取结果.项目进度"
```

旧的 `PROGRESS_PATH`（指向 `分析结果.综合总结`）不再需要，因为 `overall_progress` 字段直接用 `normalize_text_value` 的通用 `stringify_text_part` 路径即可。

### 4.3 新增 progress 类型处理

在 `normalize_cell_value()` 函数中，`date` 类型处理之前插入：

```python
if field_type == "progress":
    if isinstance(value, (int, float)) and not isinstance(value, bool):
        return value
    if isinstance(value, str):
        try:
            num = float(value)
            return num
        except ValueError:
            raise ValueError(f"invalid progress value for field: {field_name}")
    raise ValueError(f"invalid progress value for field: {field_name}")
```

### 4.4 修改 `normalize_text_value()`

- 删除 `if source_path == PROGRESS_PATH:` 分支（旧的进度分析文本格式化不再需要，因为 `overall_progress` 直接映射到 `综合总结` 字符串，走默认的 `stringify_text_part` 即可）。
- 保留 `RISK_DETAIL_PATH` 分支，但修改 `detail_fields` 元组为：
  ```python
  (RISK_TYPE_KEY, RISK_LEVEL_KEY, RISK_DESC_KEY, RISK_ACTION_KEY, RISK_OWNER_KEY, RISK_DUE_DATE_KEY)
  ```
- 删除 `if source_path == HIDDEN_RISK_PATH:` 分支。

### 4.5 修改 `format_risk_section()`

当前函数先用 `RISK_DESC_KEY` 开头，再循环 `detail_fields`。需要改为**按 `detail_fields` 元组的顺序**依次输出所有字段。修改后：

```python
def format_risk_section(item: dict[str, object], index: int, detail_fields: tuple[str, ...]) -> str:
    """把一条风险对象按指定顺序拼成一段文本。"""
    number = item.get(SEQ_KEY, index + 1)
    lines: list[str] = []
    for key in detail_fields:
        val = item.get(key)
        if val is not None:
            lines.append(f"{key}: {stringify_text_part(val)}")
    if not lines:
        return f"{number}. （无风险详情）"
    lines[0] = f"{number}. {lines[0]}"
    return "\n".join(lines)
```

**注意**：这个函数是通用的，不再特殊处理 `RISK_DESC_KEY`，所有字段都按 `detail_fields` 元组顺序输出。

### 4.6 修改 date 类型处理

`normalize_cell_value()` 中 `date` 类型处理改为：

```python
if field_type == "date":
    if not isinstance(value, str):
        raise ValueError(f"invalid date value for field: {field_name}")
    return value[:10]
```

### 测试修改

**`tests/test_wrp_sync_service.py`**

- 现有测试使用的 `payload` 结构（`"analysis": {}`）不受影响，因为它们只测试同步层的跳过/失败/去重逻辑，不涉及具体的 `sourcePath` 解析。
- 确保所有测试仍然通过。这些测试通过 monkeypatch 绕过了真实的字段映射，所以不需要修改。

**新增测试**（建议在 `tests/test_wrp_sync_service.py` 中追加）：

```python
def test_wrp_normalize_cell_value_progress_valid() -> None:
    from weekly_report_platform import dingtalk as svc
    meta = {"sourcePath": svc.PROGRESS_VALUE_PATH}
    assert svc.normalize_cell_value("项目进度", "progress", 0.75, meta) == 0.75
    assert svc.normalize_cell_value("项目进度", "progress", "0.5", meta) == 0.5

def test_wrp_normalize_cell_value_date_truncates() -> None:
    from weekly_report_platform import dingtalk as svc
    meta = {"sourcePath": "processed_at"}
    result = svc.normalize_cell_value("邮箱抽取日期", "date", "2026-05-14T08:30:00+00:00", meta)
    assert result == "2026-05-14"

def test_wrp_format_risk_section_follows_detail_fields_order() -> None:
    from weekly_report_platform import dingtalk as svc
    item = {
        "序号": 1,
        "风险类型": "技术风险",
        "风险等级": "高",
        "风险描述": "服务器延迟",
        "风险措施和最新进展": "已采购",
        "风险责任人": "张三",
        "计划解决日期": "2026-06-01",
    }
    result = svc.format_risk_section(item, 0, (
        svc.RISK_TYPE_KEY, svc.RISK_LEVEL_KEY, svc.RISK_DESC_KEY,
        svc.RISK_ACTION_KEY, svc.RISK_OWNER_KEY, svc.RISK_DUE_DATE_KEY,
    ))
    assert result.startswith("1. 风险类型: 技术风险")
    assert "风险等级: 高" in result
    assert "风险描述: 服务器延迟" in result
    assert "计划解决日期: 2026-06-01" in result
```

### 验收标准

- `dingtalk.py` 中不再有 `HIDDEN_RISK_PATH`、`RISK_SUMMARY_KEY`、`PROGRESS_PATH`。
- `normalize_cell_value()` 支持 `progress` 类型，返回值是 `float`。
- `normalize_cell_value()` 中 `date` 类型截取前 10 位。
- `format_risk_section()` 按 `detail_fields` 元组顺序输出，不再特殊处理 `RISK_DESC_KEY`。
- `normalize_text_value()` 只保留 `RISK_DETAIL_PATH` 一个特殊分支。
- 新增的 3 个测试通过。
- `test_wrp_sync_service.py` 中所有原有测试通过。

---

## 阶段 5：清理测试中引用已删除模块的部分

### 修改文件

- `tests/test_wrp_mail_analysis_service.py`
- `tests/test_wrp_prompts.py`（已在阶段 2 处理）

### 5.1 test_wrp_mail_analysis_service.py

检查 `test_wrp_analyze_email_returns_payload_and_writes_output_file` 中的 fake `process_email_analysis_with_errors` 返回值。该返回值当前只包含 `进度抽取结果`，不包含 `隐藏风险`，无需修改。

检查 `test_wrp_analyze_email_marks_llm_extraction_errors_as_analysis_error` 中的错误消息，确认不依赖隐藏风险错误，无需修改。

### 5.2 验证其他测试中无隐藏风险引用

运行以下命令确认：

```bash
grep -rn "hidden_risk\|隐藏风险\|HIDDEN_RISK" tests/
```

如有残留引用，逐个删除或调整。

### 验收标准

- 全局搜索 `隐藏风险`、`HIDDEN_RISK`、`hidden_risk` 在 `tests/` 目录下无残留引用。
- `tests/test_wrp_mail_analysis_service.py` 中所有测试通过。

---

## 阶段 6：全量测试 + 回归验证

### 执行全量测试

```powershell
python -m pytest -q --import-mode=importlib
```

### 验收标准

- **全部测试通过**（当前基线：55 passed）。
- 预期变化：删除 1 个隐藏风险测试（阶段 2），新增 3 个 dingtalk 测试（阶段 4），总数大约 57 passed。
- 手动验证 `config.local.json` 的 `targets` 中 `baseId` 和 `tableId` 与新表一致。

### 人工验收清单

| 检查项 | 状态 |
|---|---|
| `config.local.json` 11 个字段完整正确 | 待验 |
| `targets` 指向 `XD9FUh1` / `ZX6GRezwJl7DbxOxirwj3wGzVdqbropQ` | 待验 |
| `prompts.py` 进度抽取包含 7 个新字段 + 进度数值说明 | 待验 |
| `prompts.py` 风险详情包含 7 个字段（类型/等级/描述/措施/责任人/计划日期） | 待验 |
| 隐藏风险三段已删除（函数、常量、调用、测试） | 待验 |
| `dingtalk.py` 支持 `progress` 类型 | 待验 |
| `dingtalk.py` date 类型截取 YYYY-MM-DD | 待验 |
| `dingtalk.py` 风险格式化按新顺序 | 待验 |
| `config.py` `build_default_fields()` 同步更新 | 待验 |
| 全部测试通过 | 待验 |

---

## 给 AI 的执行提示

1. **严格按阶段顺序执行**，每完成一个阶段就停止，等待人工验收。
2. 每个阶段完成后运行该阶段相关的测试确认通过。
3. 不要跨阶段合并修改，即使改动很小。
4. 不要修改 `service.py`、`email_parser.py`、`llm_client.py`、`runtime.py`、前端文件。
5. 修改时遵循 `docs/ai/project/rules.md` 中的代码风格约定。
6. 所有 JSON 文件写入使用 `ensure_ascii=False` 和可读缩进。
7. 注释只解释业务规则和边界条件，不要写"代码在做什么"的注释。
8. 如果某个阶段的验收未通过，不要擅自推进到下一阶段，先报告问题。
